export default class CountryBlock {
  constructor(input, list, tabsBlock) {
    this.DOMElement = document.getElementById('country');
    this.input = input;
    this.list = list;
    this.tabsBlock = tabsBlock;
  }
}
